# 涉外案例查询与法律知识系统（uni-app）

独立的 Vue 3 + TypeScript + uni-app 微信小程序前端骨架，使用 Composition API、Pinia、SCSS 和本地 Mock 数据。

## 启动

```bash
npm install
npm run dev:mp-weixin
```

将编译产物导入微信开发者工具即可预览。后端接入时，在 `src/api/request.ts` 中替换 Mock 方法即可。
